// Dark mode is applied statically via the `dark` class on <html> in layout.tsx.
// This file is kept as a placeholder and does nothing.
export function ThemeProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
